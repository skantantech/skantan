import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Skantan Technologies Corporation | Engineering the Intelligent Enterprise",
  description: "Transform your business with innovative technology solutions. We combine deep engineering expertise with cutting-edge AI, data analytics, and digital engineering.",
  keywords: "AI, Digital Transformation, Cloud Solutions, Data Analytics, Software Development, IT Consulting",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
